spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jwt_test
    userNAME: sa
    password:
    driver-class-NAME: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  config:
    import: application-oauth.yml

